# Release zip (upload as GitHub Release asset instead of committing)
ClearBlueSky-*.zip

# Local backup (v6.4 before C# port)
backup-v6.4/

# .NET/WPF build artifacts (ClearBlueSkyWin desktop prototype)
ClearBlueSkyWin/

# Sample/user report PDFs in project root (do not commit)
/Trend_Scan_Report_*.pdf

# User data – never commit (contains optional API keys and preferences)
scanner/user_config.json
scripts/voice_choice.txt
screenshots/

# Generated / runtime
scanner/error_log.txt
scanner/__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Reports and scan outputs (user-generated)
scanner/reports/
scanner/scans/
scanner/scanner_output/

# RAG index and backtest DB (user-generated; do not ship)
scanner/rag_store/
scanner/backtest_signals.db

# Updater backup and manifest (local; user-specific)
scanner/update_backups/
scanner/update_backup_manifest.json

# User preset exports (do not commit)
scanner/scanner_presets_export.json

# Security — NEVER commit files that may contain API keys
CURSOR_RAG_KNOWLEDGE_ARCHITECTURE.md
**/api_keys*
**/api-keys*
**/.env
**/.env.*
**/secrets.*
**/mcp.json
mcp-servers/

# Cursor IDE rules (project-local, not for distribution)
.cursor/

# PTM daemon logs (Cursor project only)
scanner/ptm_logs/

# PTM rotation state (position data — Cursor project only)
scanner/ptm_rotation_state.json

# Trading (Cursor project only — not scanner app)
scanner/alpaca_trades.py
scanner/paper_trading_manager.py
scanner/alpaca_swing_dip_strategy.py
scanner/PaperTradingManager.bat

# OS
.DS_Store
Thumbs.db

# RAG/vector DB (user-generated)
velocity_memory/chroma_db/

# IDE
.idea/
.vscode/
*.swp
*.swo
